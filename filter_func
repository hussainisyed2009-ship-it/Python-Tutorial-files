# filter() = creates a collection of elements from an iterable for which a function returns true
#filter(function, iterable)

friends = [("Rachel", 19),
           ("Monica", 18),
           ("Phoebe", 17),
           ("Joey", 16),
           ("Chandler", 21)]

age = lambda data:data[1>=18] # filter()

of_age = list(filter(age, friends))

for i in of_age:
    print(i)