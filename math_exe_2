import math

radius = float(input("Enter the radius of the circle: ")) # radius input

area = math.pi * radius ** 2 # area calculation

print(f"The area of the circle is {round(area, 2)}cm²") # output with rounding to 2 decimal places