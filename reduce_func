# reduce() = apply a funtion to an iterable and reduce it to a single cumulative value.
#            performs function on first two elements and repeats process until 1 value remains
#
# reduce(funtion, iterable)

import functools

letters = ["T", "A", "H", "A"] # first letter is x and next is y and so on

name = functools.reduce(lambda x,y,:x+y, letters) # combines the letters
print(name)

factorial = [5,4,3,2,1]

result = functools.reduce(lambda x,y,:x * y, factorial)

print(result)