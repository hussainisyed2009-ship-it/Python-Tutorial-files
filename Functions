# functions: A block of reusable code
    # place() after the function name to invoke it

def happy(name, age): # basically makes a preset and name is created as a variable place holder
    print(f"Heppy Birth day{name}")
    print(f"How old will you be {age}")
    print("lil johny")

happy("taha", 16) #invokes function or runs it and need matching set of parameters


def display_invoice(username, amount, due_date):
    print(f"Hello {username}")
    print(f"Your bill of ${amount:.2f} is due {due_date}")

display_invoice("taha", 500000000, "first of feb")


