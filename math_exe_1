import math

radius = float(input("Enter the radius of the circle: ")) # radius input

circumference = 2 * math.pi * radius # circumference calculation

print(f"The circumference of the circle is {round(circumference, 2)}cm") # output with rounding to 2 decimal places
